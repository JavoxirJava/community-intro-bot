# Exact path on flip-card-app.javohir-dev.uz (existing Let's Encrypt).
# Do not send Mini App traffic here. No X-Frame-Options DENY.
location = /community-intro/webhook {
    include snippets/flashcards-proxy.conf;
    proxy_pass http://127.0.0.1:8788;
}
